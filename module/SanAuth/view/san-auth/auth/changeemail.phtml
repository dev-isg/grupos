<script type="text/javascript" src="<?php echo $this->host('ruta')?>/js/jquery.validate.min.js"></script>
<div class="main-container">
	<div class="container main wrapper clearfix">
		<?php if($this->mensaje[0]){?>
		<div class="row-fluid">
			<div class="alert alert-success alert-message" style="margin-bottom:0;margin-top:20px;">
				<button type="button" class="close" data-dismiss="alert">×</button>
				<?php echo $this->mensaje[0];?>
			</div>
		</div>
		<?php }?>
		<div class="row-fluid">
			<div class="span12 recuperacion group-sidebar">
	            <h1>Recuperación de Contraseña</h1>
	            <small>
	                Si ha olvidado su contraseña, introduzca su dirección de correo electrónico debajo y le enviaremos las instrucciones para cambiar su contraseña.
	            </small>
                    <?php
                            $form = $this->form;
                            $form->setAttributes(array(
                                'action' => $this->url.'/cambio', 
                                'method' => 'post',
                                'id' => 'recuperacion'
                            ));

                            $form->prepare();
                            echo $this->form()->openTag($form);
                    ?>
	                <p>
	                <label for="">Correo Electrónico</label>
                         <?php echo $this->formRow($form->get('va_email'));?>
	                </p> 
                          <?php echo $this->formSubmit($form->get('submit'));?>                 
	            </form>
	        </div>      
		</div>
	</div>
</div> <!-- #main-container -->
<script type="text/javascript">
	$(document).ready(function(){
		$(".alert-message").alert();
		window.setTimeout(function() { $(".alert-message").alert('close'); }, 7000);

		$("#recuperacion").validate({
            rules: {
                va_email:{
	                required : true,
	                email:8             
	            }
            },
            messages: {
              	va_email: {
	                required : "Ingrese su correo electronico",
	                email:"Debe ingresar un correo valido"
	            }
            }
        });
	});
</script>